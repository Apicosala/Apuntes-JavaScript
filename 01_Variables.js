/*  ### VARIABLES ###
Los nombres de las variables han de comenzar por una letra, el símbolo $ o _ y distingue entre mayúsculas y minúsculas.
Tipos de variables:
        -number.
        -boolean (True/False).
        -String (Cadena de caracteres).
        -Undefined
*/

let midato; // undefined: no definimos si midato es un número, un frase o palabra (string), o un bool
typeof (midato)
let numEntero = 5; // number.
typeof (numEntero)
let numDecimal = 5.25; // number.
typeof (numDecimal)
let si = true; // boolean.
typeof (si)
let no = false; // boolean.
typeof (no)
let dato = "Hola"; // string.
typeof (dato)
let frase = "Hola Mundo!!" // string.
typeof (frase)
console.log(frase)
